[package]
name = "hirelevel"
version = "1.0.0"
description = "HireLevel desktop app"
authors = ["HireLevel contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
